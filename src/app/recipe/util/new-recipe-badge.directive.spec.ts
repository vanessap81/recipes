import { NewRecipeBadgeDirective } from './new-recipe-badge.directive';

describe('NewRecipeBadgeDirective', () => {
  it('should create an instance', () => {
    const directive = new NewRecipeBadgeDirective();
    expect(directive).toBeTruthy();
  });
});
